import { DiscountsByRecipientPipe } from './discounts-by-recipient.pipe';

describe('DiscountsByRecipientPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountsByRecipientPipe();
    expect(pipe).toBeTruthy();
  });
});
